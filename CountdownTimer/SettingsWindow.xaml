<Window x:Class="Btl.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:my="clr-namespace:Btl.View"
        Title="SettingsWindow"
        Width="400"
        Height="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <my:SettingsView x:Name="settingsView1" />
        <Button Content="OK" Grid.Column="1" Grid.Row="1" Name="button1" />
        <Button Content="Cancel" Grid.Row="1" Name="button2" />
    </Grid>
</Window>
